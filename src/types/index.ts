export type Todo = {
  id: string;
  title: string;
  body: string;
  isDone: boolean;
};
export type Todos = Array<Todo>;
export type Info = {
  name: string;
  description: string;
  image: string;
};
